gospel-fix-automation/
│
├── 📄 AI_AUTOMATION_WORKFLOW.md      # 전체 아키텍처 & 워크플로우
├── 📄 QUICK_START.md                 # 4-6시간 MVP 완성 가이드
├── 📄 README.md                      # 프로젝트 소개
├── 📄 package.json                   # npm 의존성
├── 📄 tsconfig.json                  # TypeScript 설정
├── 📄 next.config.ts                 # Next.js 설정
├── 📄 .env.local                     # 환경변수 (비공개)
├── 📄 .gitignore
│
├── 📁 app/                           # Next.js App Router (프론트 + API)
│   │
│   ├── 📁 api/                       # API 라우트들
│   │   ├── 📁 emails/
│   │   │   ├── route.ts              # 이메일 목록 조회
│   │   │   ├── send-response.ts      # 자동 응답 발송
│   │   │   └── webhook.ts            # Gmail 웹훅
│   │   │
│   │   ├── 📁 classify/
│   │   │   ├── route.ts              # 데이터 분류 API
│   │   │   └── batch.ts              # 배치 분류
│   │   │
│   │   ├── 📁 cron/
│   │   │   ├── email-sync.ts         # 이메일 정기 동기화
│   │   │   └── classify-batch.ts     # 배치 분류 정기 실행
│   │   │
│   │   └── 📁 auth/
│   │       ├── gmail.ts              # Gmail OAuth
│   │       └── callback.ts           # OAuth 콜백
│   │
│   ├── 📁 dashboard/                 # 대시보드 페이지들
│   │   ├── 📁 emails/
│   │   │   ├── page.tsx              # 이메일 자동응답 관리
│   │   │   └── layout.tsx
│   │   │
│   │   ├── 📁 data-classify/
│   │   │   ├── page.tsx              # 데이터 분류 관리
│   │   │   └── layout.tsx
│   │   │
│   │   ├── page.tsx                  # 대시보드 메인 페이지
│   │   └── layout.tsx                # 대시보드 레이아웃
│   │
│   ├── layout.tsx                    # 루트 레이아웃
│   └── page.tsx                      # 홈페이지
│
├── 📁 lib/                           # 비즈니스 로직 (서비스 레이어)
│   ├── db.ts                         # MySQL 연결 & 쿼리 함수
│   ├── openai.ts                     # OpenAI API 래퍼
│   ├── gmail.ts                      # Gmail API 클라이언트
│   ├── email-service.ts              # 이메일 자동 응답 로직
│   ├── classify-service.ts           # 데이터 분류 로직
│   └── utils.ts                      # 유틸리티 함수들
│
├── 📁 components/                    # React 컴포넌트들
│   ├── EmailDashboard.tsx            # 이메일 관리 컴포넌트
│   ├── ClassifyDashboard.tsx         # 분류 관리 컴포넌트
│   ├── StatCard.tsx                  # 통계 카드
│   ├── LoadingSpinner.tsx            # 로딩 스피너
│   └── Header.tsx                    # 헤더
│
├── 📁 types/                         # TypeScript 타입 정의
│   └── index.ts                      # 모든 타입 정의 (Email, Classification 등)
│
├── 📁 styles/                        # CSS 파일들
│   ├── globals.css                   # 글로벌 스타일
│   └── variables.css                 # CSS 변수
│
├── 📁 database/                      # 데이터베이스 관련
│   ├── schema.sql                    # MySQL 테이블 스키마
│   └── 📁 migrations/                # 마이그레이션 파일들
│       ├── 001_initial_schema.sql
│       └── 002_add_indexes.sql
│
├── 📁 scripts/                       # 유틸리티 스크립트들
│   ├── setup-db.ts                   # DB 초기화
│   ├── seed-data.ts                  # 테스트 데이터 생성
│   └── create-project.sh             # 프로젝트 생성 스크립트
│
├── 📁 public/                        # 정적 파일 (이미지, 폰트 등)
│   ├── logo.png
│   └── favicon.ico
│
└── 📁 node_modules/                  # npm 패키지들 (npm install 후 자동 생성)

═══════════════════════════════════════════════════════════════════════

🔑 핵심 구조 설명:

1️⃣  app/api/          → API 엔드포인트 (서버)
2️⃣  app/dashboard/    → 웹 페이지 (클라이언트)
3️⃣  lib/              → 비즈니스 로직 (양쪽에서 사용)
4️⃣  components/       → React 컴포넌트
5️⃣  database/         → DB 스키마
6️⃣  types/            → 타입 정의

═══════════════════════════════════════════════════════════════════════

📊 파일 개수 목표:

Frontend (TSX):  ~10 파일
API Routes:      ~8 파일
Services (TS):   ~5 파일
Types (TS):      ~1 파일
Config:          ~4 파일
Database:        ~1 파일 (schema.sql)
──────────────
총합:            ~29 파일

═══════════════════════════════════════════════════════════════════════

🚀 데이터 흐름:

사용자 입력 (대시보드)
    ↓
React 컴포넌트 (app/dashboard)
    ↓
API 라우트 (app/api)
    ↓
비즈니스 로직 (lib/)
    ↓
외부 API (OpenAI, Gmail)
    ↓
MySQL 데이터베이스
    ↓
대시보드에 결과 표시

═══════════════════════════════════════════════════════════════════════

✅ 다음 단계:

1. gospel-fix-automation 디렉토리에서 npm init
2. package.json 의존성 설치
3. .env.local 환경변수 입력
4. database/schema.sql로 MySQL 테이블 생성
5. lib/ 파일들 작성
6. app/ 페이지/API 구현
7. npm run dev로 실행
